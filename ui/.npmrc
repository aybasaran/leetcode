# pnpm 11: .npmrc holds ONLY auth/registry settings. All other config lives in
# pnpm-workspace.yaml. Pinning the registry explicitly prevents a stray env var
# or inherited config from silently redirecting installs to another registry.
registry=https://registry.npmjs.org/
